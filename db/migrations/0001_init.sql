-- =====================================================================
-- Migration 0001 — Initial schema (v0.1)
-- Additive-only policy: migration berikutnya HANYA menambah (TDD §27).
-- Cara pakai (Supabase SQL Editor / psql):
--   1) jalankan isi db/schema.sql      (atau salin migration ini)
--   2) jalankan db/seed/difficulties.sql
-- File ini = salinan schema.sql untuk pelacakan versi migrasi.
-- =====================================================================

-- Untuk v0.1, isi migration identik dengan db/schema.sql.
-- Jalankan db/schema.sql lalu db/seed/difficulties.sql.
-- (Migration runner formal ditambahkan saat CI/CD matang — TDD §26.)

\echo 'Jalankan db/schema.sql kemudian db/seed/difficulties.sql'
